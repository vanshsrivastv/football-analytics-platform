export default function HomePage() {
  return (
    <main className="min-h-screen flex items-center justify-center">
      <p className="text-[var(--color-text-secondary)] text-sm">
        matchIQ — scaffolding complete. Next: seed the database.
      </p>
    </main>
  );
}